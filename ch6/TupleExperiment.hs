module TupleExperiment where

-- quotRem :: Integral a => a -> a -> (a, a)
-- returns tuple of the quotient & remainder of two Integrals
--
-- divMod :: Integral a => a -> a -> (a, a)
-- return tuple of quotient & modulus of two Integrals
